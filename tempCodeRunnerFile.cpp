#include <bits/stdc++.h>
#define rep(i,n) for (int i=0; i < (n); ++i)
#define all(v) v.begin(),v.end()
using namespace std;
using ll = long long;

/**/
struct st {
    vector<int> num(3);
};

int main(){
    st s;
    s.num[0] = 100;
    cout << s.num[0] << endl;
    return 0;
}